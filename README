This user-space checkpointing library has been developed while I was student at Moscow State University. I haven't updated the sources for long time, so it doesn't compile under current Linux systems.

Could be used as an example or starting point for user-space checkpointing solution.
